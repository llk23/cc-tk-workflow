# 工作流生命周期

```
DRAFT (草稿) ──保存──▶ ACTIVE (激活) ──执行──▶ Pipeline 实例
    ▲                     │                        │
    │                     │                        ├── PENDING (排队中)
    └──编辑──              ├──暂停──▶ PAUSED        ├── RUNNING (运行中)
                           │                        ├── COMPLETED (完成)
                           └──归档──▶ ARCHIVED      ├── FAILED (失败)
                                                    └── CANCELLED (取消)
```
